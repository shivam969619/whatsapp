import { MongoClient } from 'mongodb';
import { MulterGfsOptions } from 'multer-gridfs-storage/lib/types/multer-gfs-options';
import { DbTypes } from 'multer-gridfs-storage/lib/types/db-types';
export interface DbStorageOptions<T = DbTypes> extends MulterGfsOptions {
    db: T | Promise<T>;
    client?: MongoClient;
}
